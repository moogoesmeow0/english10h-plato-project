# Taran's English 10H Plato Project

the file that is compiled into the slideshow is `main.typ`
the python script that collects the landmark scripts into a csv file is in the `monitor` folder
the rust program that performs the digital signal processing on the csv file is in the `fourier` folder, and the code is under the `src` directory within that
